{
  "name": "picks-and-bans-server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "heroku-postbuild": "cd app && npm install && npm install --only=dev --no-shrinkwrap && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server",
    "build": "cd app && npm install && npm run build"
  },
  "author": "SnowKid",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.17.1",
    "hirez-api-https": "^1.0.0",
    "http": "0.0.1-security",
    "md5": "^2.3.0",
    "moment": "^2.29.4",
    "node-fetch": "^2.6.9",
    "path": "^0.12.7",
    "sockjs": "^0.3.21"
  }
}
